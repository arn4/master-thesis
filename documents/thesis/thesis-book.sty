% \newlength{\PaperWidth}{210mm}
% \newlength{\PaperHeight}{297mm}
% \newlength{\BindingOffset}{0mm}

\ifthenelse{\boolean{ForPrinting}}{
  % Printable version
  \RequirePackage[
    paper=a4paper,
    textwidth=140mm,
    textheight=210mm,
    marginratio={4:6,5:5},
    bindingoffset=2mm
    % showframe % testing purpose
  ]{geometry}

  \RequirePackage[hidelinks]{hyperref}
  \newcommand{\Href}[2]{#2\footnote{\url{#1}}}

}{% Digital version
  \RequirePackage[
    paper=a4paper,
    textwidth=140mm,
    textheight=210mm,
    marginratio={4:6,5:5},
    % showframe % testing purpose
  ]{geometry}

  \RequirePackage[
    colorlinks,
    linkcolor=blue,
    urlcolor=magenta,
    citecolor=green,
    bookmarks
  ]{hyperref}

  \newcommand{\Href}[2]{
    \href{#1}{#2}
  }
}

\RequirePackage[section]{placeins}
\RequirePackage[indent=0cm]{parskip}


% Fonts
\RequirePackage{anyfontsize}
% \RequirePackage{libertine}


\RequirePackage{lipsum}
\RequirePackage{todonotes}

% Theorems
\theoremstyle{plain}
\newtheorem{Theorem}{Theorem}[chapter]
\newtheorem{Lemma}{Lemma}[chapter]

\theoremstyle{definition}
\newtheorem{Definition}{Definition}[chapter]
\newtheorem{Example}{Example}[chapter]

% Header and footer
\RequirePackage{titlesec}
\newpagestyle{ruled}[\normalsize]{
 \sethead[\thepage][][\MakeUppercase{Chapter \thechapter.\space\chaptertitle}]  % even
   {\MakeUppercase{\thesection.\space\sectiontitle}}{}{\thepage} % odd
 \headrule
 \setfoot{}{}{}
}
\newpagestyle{appendix-ruled}[\normalsize]{
 \sethead[\thepage][][\MakeUppercase{Appendix \thechapter.\space\chaptertitle}]  % even
   {\MakeUppercase{\thesection.\space\sectiontitle}}{}{\thepage} % odd
 \headrule
 \setfoot{}{}{}
}

\newpagestyle{acknowledgments}[\normalsize]{
 \sethead[\thepage][][\MakeUppercase{\chaptertitle}]  % even
   {\MakeUppercase{}}{}{\thepage} % odd
 \headrule
 \setfoot{}{}{}
}
